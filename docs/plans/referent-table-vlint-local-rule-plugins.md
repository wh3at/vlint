# vlint Local Rule Plugins — Referent Table

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
| --- | --- | --- | --- | --- | --- | --- |
| ユーザー要望 | プロジェクト固有のレイアウト契約をvlintで検査可能にする | vlint導入先リポジトリが明示指定する、単一TypeScriptファイルの検査定義 | 手段 | 設定読込 → ルール読込 → 対象検査 → 結果集計 | ローカルルールプラグインとは、導入先リポジトリ内だけで使用する信頼済みの検査定義を指す | ローカルルールプラグイン |
| ユーザー回答 | プロジェクト固有ルールの入力を固定する | DOM、computed style、位置・サイズ、target・device情報、run全体の観測結果 | 値 | 対象検査 → 結果集計 | ルール入力とは、ローカルルールプラグインが判定に利用できるvlint提供情報を指す | ルール入力 |
| ユーザー回答 | 固有設定を読込時に検証する | ローカルルールプラグインが公開し、vlintが設定読込時に照合する型とschema | 手段 | ルール読込 → 設定検証 → 対象検査 | プラグイン設定schemaとは、ルール固有設定の許容形を宣言する契約を指す | プラグイン設定schema |
| 既存vlint契約 | 組み込みルールとローカルルールの結果を同じrunへ統合する | 違反、実行失敗、未実行、terminal/JSON出力、run集計、終了コード | 記録 | 対象検査 → 結果集計 → 終了判定 | 統合ルール結果とは、由来にかかわらず既存run契約へ格納されたルール結果を指す | 統合ルール結果 |
| ユーザー選択 | 信頼境界を明示する | 設定で指定されたローカルルールプラグインとしてvlintの組み込みBun runtimeが実行するコード | 手段 | ルール読込 → ルール実行 | 信頼済みローカルコードとは、利用者がリポジトリの他の実行コードと同じ責任で承認するコードを指す | 信頼済みローカルコード |
| 計画判断 | ローカルルールとvlintの接続面を固定する | 設定schema、browser evaluator、run finalizer、contract versionをまとめた公開契約 | 記録 | ルール読込 → 契約検証 → 対象検査 | プラグイン契約とは、vlint最新版とローカルルールプラグインが共有する公開入出力を指す | プラグイン契約 |
| 計画判断 | Playwright内部を公開せずDOM検査能力を渡す | 対象page、設定、target、deviceを使ってbrowser内で評価される直列化可能な入力 | 値 | 対象準備 → browser評価 → 結果検証 | ルールコンテキストとは、ローカルルールのbrowser evaluatorへvlintが渡す検査入力を指す | ルールコンテキスト |
| 計画判断 | プロジェクト固有の診断を安定したJSONへ格納する | message、locator、geometry、上限付きdetailsを持つ共通の違反記録 | 記録 | browser評価 → 結果検証 → 統合出力 | ローカル違反エンベロープとは、プロジェクト固有の違反を統合結果へ格納する共通形を指す | ローカル違反エンベロープ |
